name: CodeQL

on:
  pull_request:
  push:
    branches:
      - main
  schedule:
    - cron: "17 3 * * 1"

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    uses: malabie/github-actions-standards/.github/workflows/reusable-codeql-js.yml@main
    secrets: inherit
